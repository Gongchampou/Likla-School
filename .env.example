# Copy this file to .env (or .env.local) and fill in your values
# Do NOT commit your real secrets. .env is gitignored.

# Firebase web app config (client-safe, but avoid committing real project values)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
# Typically: <project-id>.appspot.com
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Supabase (anon key is public; still avoid committing)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# If you re-introduce AI keys, DO NOT expose private server keys in client bundle
# GEMINI_API_KEY=
